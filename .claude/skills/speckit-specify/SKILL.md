---
name: speckit-specify
description: 從自然語言描述產生 feature spec.md，包含 User Stories、Acceptance Scenarios、Requirements、Success Criteria。spec-kit SDD 工作流第二步。
---

Read the full command instructions from `.claude/commands/speckit.specify.md` and execute the workflow defined there.

The command file contains the complete implementation guide. Follow it exactly, including:
- Pre-execution checks (extension hooks)
- All outline steps in order
- Post-execution checks

After reading the command file, proceed immediately without restating these instructions.
